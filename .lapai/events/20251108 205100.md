---
kind: "[[ai/registry/PromptAccepted]]
---

# Prompt

Improve the GitHub workflow so after it copies the files, it checks, what are the versions of the dependencies marked with `workspace:*`. Check corresponding `package.json` in the pnpm package module. Replace the version.
